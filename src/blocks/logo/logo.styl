@import('../../variables.styl')

.logo
    position: absolute
    top: 13px
    left: $layoutGapHorizontal

    &__inno
        width: 206px
        height: 40px

        float: left

    &__js
        border: 1px solid rgba(0, 0, 0, .3)
        float: left
        box-sizing: border-box

        margin: -6px 0 0 10px
